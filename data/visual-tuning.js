/* Captain Salvage 시각 튜닝 단일 소스. 게임 밸런스 수치와 섞지 않는다. */
window.CaptainSalvageVisuals = Object.freeze({
  shield: Object.freeze({
    /* 인덱스는 현재 방어막 레이어(0~5). */
    layerOpacity: [0, .24, .38, .52, .66, .8],
  }),
});
